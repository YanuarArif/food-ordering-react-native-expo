import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AkunTabs = () => {
  return (
    <View>
      <Text>AkunTabs</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default AkunTabs;
